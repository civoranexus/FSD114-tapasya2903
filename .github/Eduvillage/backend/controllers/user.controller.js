exports.getAllUsers = (req, res) => {
  res.status(200).json({
    message: "Get all users API (logic to be added)",
  });
};

exports.createUser = (req, res) => {
  res.status(201).json({
    message: "Create user API (logic to be added)",
  });
};
